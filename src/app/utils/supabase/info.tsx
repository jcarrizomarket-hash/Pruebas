/**
 * Re-exporta la configuración de Supabase desde el archivo centralizado
 * Este archivo mantiene compatibilidad con imports existentes
 */

export { projectId, publicAnonKey } from '../../config/supabase.config';